#include"main.h"

/**
 * print_last_digit - function to print last digit of any given integer
 * @x: integer type
 * Return: Always 0 on success
 */

int print_last_digt(int x)
{
	int print_last_digit = x;

	if (x % 10)
	{
		return (print_last_digit);
	}
	return (0);
}
