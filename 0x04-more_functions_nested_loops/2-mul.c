#include "main.h"

/**
 * mul - Multiplies two integers
 * @a: The first to multiply
 * @b: The second number to multiply with the first numbers
 * Return: The result of the multiplication of a param and b param
 */

int mul(int a, int b)
{
	return (a * b);
}
