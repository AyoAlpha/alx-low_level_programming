#include "main.h"

/**
 * mul - functions that multiply two integer
 * @a: first integer to be considered
 * @b: second interger to be considered
 * Return: the product the two integer
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
