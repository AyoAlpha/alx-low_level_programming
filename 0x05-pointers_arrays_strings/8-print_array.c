#include "main.h"
#include <stdio.h>

/**
 * print_array - function that print array
 * @a: parameter to be used
 * @n: number of elemen
 * Return: nothing
 */
void print_array(int *a, int n)
{
	if (a == n)
	{
		printf("%d, \n", a);
	}
}
