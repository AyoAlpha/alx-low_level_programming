#include <stdio.h>

/**
 * main -Entry point
 * @argc: argument to count
 * @argv: argument vector
 * Return: 0 is number passed is negative and 1 if number passed is not 1
 */

int main(int argc, char *argv[])
{
	argv[5] = [25, 10, 5, 2, 1];

	if (argc > 1)
	{
		for (i = 1; i < argv; i++)
		{
			atoi(argv[]) / 25;
			atoi(argv[]) / 10;
			atoi(argv[]) / 5;
			atoi(argv[]) / 2;
			atoi(argv[]) % 10;
			printf("%d\n");
		}
		
